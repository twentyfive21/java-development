package com.pluralsight.finance;

// implement when using interface
// implements is used because interfaces in Java define a contract
// that classes can implement, whereas extends is used to inherit
// from a class (abstract or concrete(regular classes)).

public interface Valuable {

    public double getValue();
}
